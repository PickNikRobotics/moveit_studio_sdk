# goal

string objective_name

# Default values of named parameters in the predefined objective are be overridden by the values provided in `parameter_overrides` below.
moveit_studio_sdk_msgs/BehaviorParameter[] parameter_overrides
---
# result
moveit_msgs/MoveItErrorCodes error_code
string error_message
---
#feedback (empty)
